# CORE — Canopy-Occluded Road Extraction
# Install: pip install -r requirements.txt
#
# CPU-only (local, no GPU):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
#
# GPU (Colab / CUDA):
#   pip install torch torchvision
#   pip install -r requirements.txt

# Deep learning
torch>=2.0.0
torchvision>=0.15.0

# Image processing
opencv-python>=4.8.0
Pillow>=9.5.0
albumentations==1.4.18      # pinned — API changed in 2.x

# Scientific computing
numpy>=1.24.0
scipy>=1.10.0
scikit-image>=0.21.0        # required for skeleton-based gap bridging

# Training utilities
tqdm>=4.65.0
pyyaml>=6.0

# Optional experiment tracking
# tensorboard>=2.13.0
# wandb>=0.15.0
